import {connect} from './cdp.mjs';const c=await connect();try{console.log(await c.evaluate(`(async()=>{const p=app.plugins.plugins['md-palette'];await p.mainGroup.children[p.mainGroup.currentTab].openFile(app.vault.getAbstractFileByPath('Folder-Review/Performance.md'));await p.changeFolders(s=>{s.current='';s.display='list'});p.selectView('link','folder');return true})()`));await c.send('Profiler.enable');await c.send('Profiler.start');console.log(await c.evaluate(`(()=>{const t=performance.now();app.plugins.plugins['md-palette'].render();return{render:performance.now()-t,rows:document.querySelectorAll('.mdp-folder-item').length}})()`));const {profile}=await c.send('Profiler.stop');const hits=new Map();for(const n of profile.nodes)if(n.hitCount)hits.set(n.id,{name:n.callFrame.functionName,url:n.callFrame.url.slice(-80),hits:n.hitCount});console.log([...hits.values()].sort((a,b)=>b.hits-a.hits).slice(0,15));}finally{c.close()}
