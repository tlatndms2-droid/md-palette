# MD Palette 0.1.4 — 메타데이터 글자 크기와 Sub 드래그 개선

Metadata View 안에서 Ctrl+휠 위로 글자를 키우고, Ctrl+휠 아래로 줄일 수 있습니다. 10~32px 범위에서 1px씩 바뀌며 재시작 후에도 유지됩니다. 각주·문맥·메타데이터 내용과 각주 편집창에 적용됩니다.

Sub 문서에서 다른 줄로 이동하자마자 놓으면 삽입이 취소되던 드래그 문제를 수정했습니다. 드래그 위치 표시를 재사용하고, 내용이 바뀌지 않은 긴 문서 전체를 반복해서 읽는 작업을 줄였습니다.

- 일반 휠 스크롤, 각주 편집 중 입력·커서, 기존 원문·문서별 정리를 유지합니다.
- 41개 테스트·빌드, Obsidian 1.13.7 Sandbox Ctrl+휠·Sub Source mode/Live Preview 삽입·재시작 확인 통과.
- 805,407글자 Sub와 메타데이터 1,503개 시험에서 이동 처리 121회 동안 전체 문서 읽기 1회, 위치 표시 요소 2개 재사용을 확인했습니다. 해당 PC·시험 조건의 관측입니다.

## 업데이트 후 사용자 확인

- [ ] BRAT에서 0.1.4로 업데이트한 뒤 Metadata View 본문에 마우스를 올리고 Ctrl+휠 위/아래를 굴립니다. 글자가 커지고 작아져야 합니다.
- [ ] 각주 또는 Highlight를 Sub 문서의 원하는 줄로 옮겨 놓습니다. 삽입 메뉴가 나타나고 선택한 위치에 내용이 들어가야 합니다.
- [ ] Obsidian 재시작 후 선택한 글자 크기가 유지되는지 확인합니다.

---

## 이전 0.1.3 — 각주·메타데이터 Markdown 표시

Metadata View의 각주 본문과 본문 문맥, Highlights·Tasks·Block Reference·웹 링크 제목에 Markdown 서식을 표시합니다. `**굵게**`, `==강조==`, 목록과 링크를 읽기 편한 모습으로 보여주며 테마 색상을 따릅니다.

- 각주 편집에서는 Markdown 원문을 수정하고, 저장하면 서식이 적용된 표시로 돌아옵니다.
- 기존 원문 위치 이동, URL 열기 메뉴, 할 일 체크와 드래그 재사용을 유지합니다.
- Card 미리보기처럼 임베드는 링크로 표시합니다. 원문 내용은 표시만으로 바뀌지 않습니다.
- 41개 테스트·빌드, Obsidian 1.13.7 격리 Sandbox의 실제 클릭·편집·검색·드래그 및 프로세스 재시작을 통과했습니다.

## 업데이트 후 사용자 확인

- [ ] BRAT에서 MD Palette를 0.1.3으로 업데이트하고 Metadata View → 각주를 엽니다. 굵게·강조·링크가 서식으로 보여야 합니다.
- [ ] 각주의 편집 → 저장을 누릅니다. 편집창에는 원문이 보이고 저장 후에는 서식이 적용되어야 합니다.
- [ ] Obsidian을 재시작합니다. 같은 각주가 서식을 유지해 보여야 합니다.

---

## 이전 0.1.2 — 드롭 대상 줄 강조

자료를 문서로 드래그하면 실제 삽입될 줄 전체에 옅은 강조색 배경이 표시되고, 정확한 글자 위치에는 세로 커서가 함께 표시됩니다. 색상은 현재 Obsidian 강조색을 따릅니다.

- 마우스를 움직이면 대상 줄과 글자 위치가 함께 바뀝니다.
- 삽입 메뉴가 열린 동안 유지되며, 삽입·취소 후 두 표시가 함께 사라집니다.
- 빈 줄·자동 줄바꿈된 문장·문서 끝, Source mode와 Live Preview에서 확인했습니다.

---

## 이전 0.1.1 — 글자 사이 드롭 위치 표시

Main/Sub 문서로 자료를 끌어 놓을 때 실제 삽입될 글자 사이에 강조색 세로 커서가 표시됩니다. 메뉴에서 삽입 방식을 고르는 동안에도 위치를 유지합니다.

- 같은 줄의 글자 사이, 빈 줄, 자동 줄바꿈된 문장과 문서 끝을 가리킵니다.
- Esc·메뉴 취소·드래그 종료 시 필요 없는 표시를 제거합니다.
- 실제 Sandbox 드래그, Source mode·Live Preview, 프로세스 재시작 및 41개 테스트를 통과했습니다.
- BRAT 업데이트 후 0.1.1을 확인하고, Metadata 링크를 문장 중간에 놓아 세로 커서 위치에 들어가는지 확인하세요.

---

## 이전 0.1.0 — 최종 통합 검증

Main/Sub, 연결 파일 정리, 가상 폴더, Metadata와 드래그 재사용을 함께 확인한 통합 버전입니다. 기존 화면과 사용 방법은 유지됩니다.

- Obsidian 1.13.7의 격리 Sandbox에서 실제 클릭·드래그·오류 보호·업데이트·앱 재시작을 확인했습니다.
- 0.0.15에서 라벨·문서별 폴더·탭·역할과 기존 시험 파일 2,017개를 보존했습니다.
- 41개 테스트와 빌드를 통과했습니다. 연결 파일 2,000개와 Metadata 1,500개로 입력·목록 반응을 측정했습니다. 모든 PC의 성능을 보장하는 수치는 아닙니다.
- 플러그인 기능 소스 변경은 없습니다. BRAT 업데이트 확인은 사용자가 담당합니다.

## 업데이트 후 사용자 확인

- [ ] BRAT 업데이트 후 MD Palette 버전이 0.1.0인지 확인합니다.
- [ ] Main에서 연결 파일을 더블클릭합니다. 오른쪽 Sub가 열리고, 다른 파일은 같은 Sub 탭에서 바뀌어야 합니다.
- [ ] 다른 Main을 지정했다가 돌아옵니다. 문서별 가상 폴더와 라벨이 유지되어야 합니다.
- [ ] Metadata의 Task를 누르거나 자료를 Sub에 끌어 놓습니다. 선택한 동작만 반영되어야 합니다.
- [ ] Obsidian을 재시작합니다. Main/Sub와 탭·정리 상태가 유지되어야 합니다.

---

## 이전 0.0.15 — Sub 전체 높이 배치

Main이 영상 등 다른 파일과 위아래로 배치된 경우, Sub가 오른쪽 전체 높이를 사용하도록 수정했습니다.

- 왼쪽의 영상 위·Main 아래 배치를 유지하고, Sub는 묶음 전체의 오른쪽에서 열립니다.
- 이미 아래쪽에 열려 있던 Sub도 전체 높이로 정리합니다. 기존 파일과 탭은 유지합니다.
- 기본 더블클릭의 Sub 탭 교체, Ctrl+더블클릭 새 탭, Ctrl+Shift+더블클릭 새 Sub 그룹 동작을 유지합니다.

41개 테스트·빌드·Obsidian 1.13.7 Sandbox 실제 클릭·참조 배치 비교·프로세스 재시작을 통과했습니다. 기존 파일 2,065개와 라벨·가상 폴더·탭 복원을 확인했습니다. BRAT 확인은 사용자가 담당합니다.

## 업데이트 후 확인

- [ ] BRAT으로 0.0.15 업데이트 후 영상과 Main을 위아래로 놓고 연결 파일을 더블클릭합니다. Sub가 오른쪽 전체 높이로 열려야 합니다.
- [ ] 다른 파일을 더블클릭합니다. 같은 Sub에서 파일이 바뀌고 왼쪽 영상·Main 배치는 유지되어야 합니다.
- [ ] Obsidian을 재시작합니다. 왼쪽 위아래 배치와 오른쪽 Sub가 유지되어야 합니다.
