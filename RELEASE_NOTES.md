# 0.0.5 — Connections 관계 탐색

`Link View → Connections`에서 Main과 연결된 Markdown을 탐색합니다.

- 위부터 Backlinks, Outgoing Links, Local Graph를 함께 표시합니다. Outgoing에는 본문 링크와 `link note` 링크를 표시합니다.
- 목록 또는 그래프 노드를 한 번 클릭하면 선택하고, 더블클릭하면 Sub에서 엽니다. 우클릭하면 Sub / Reference를 선택합니다.
- 각 영역 제목으로 접고 펼치며, 구분선을 위아래로 드래그해 높이를 조절합니다. 높이와 접힘 상태는 재시작 후 복원됩니다. 구분선 드래그 중 Esc로 취소할 수 있습니다.
- 그래프는 휠로 확대·축소, 빈 공간 드래그로 이동합니다. 빈 공간을 더블클릭하면 전체가 보이는 위치로 돌아옵니다. 연결이 많으면 파일명은 마우스를 올리거나 선택했을 때 표시합니다.
- 상단 `연결 파일 추가`로 기존 파일을 Main의 `link note`에 연결합니다. Connections에는 Markdown만 표시합니다.
- 사용자 결정에 따라 문서 기준으로 구현했으며 검색창·필터는 추가하지 않았습니다. 기존 Card 정리 상태를 보존합니다.

타입 검사·테스트 13개·Obsidian 1.13.7 격리 Sandbox의 실제 입력·밝은/어두운 테마·220px 폭·프로세스 재시작 검증을 통과했습니다. 2,008개 시험 파일에서 목록 600행·그래프 401노드의 다시 그리기는 약 112ms였습니다. 이 값은 제한된 시험의 단일 측정이며 전체 사용자 체감 지연이나 CPU 사용률을 뜻하지 않습니다.

사용자 확인:

- [ ] BRAT에서 0.0.5로 업데이트 → `Link View → Connections`에 세 영역이 표시됩니다.
- [ ] 목록과 그래프에서 더블클릭 → Sub에서 열림. 우클릭 → Reference에서도 열 수 있습니다.
- [ ] 영역 접기·구분선 높이 조절 → Obsidian 재시작 후 유지됩니다.
- [ ] 그래프에서 휠 확대·빈 공간 드래그 → 관계를 탐색할 수 있습니다.

3단계 버전입니다. Folder·Metadata·Main 본문 카드 드롭은 후속 단계이며, 사용자 본인의 확인 후 4단계로 진행합니다.
