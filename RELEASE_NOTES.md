# MD Palette 0.1.10 — Canvas 삽입 안내 위치 개선

Canvas 삽입 안내·겹침 알림과 취소/마지막 삽입 되돌리기를 상단 왼쪽에 고정합니다. 오른쪽 기본 도구막대에 여백을 두며 하단 도구막대를 가리지 않습니다. 드래그·클릭 배치 방식은 유지합니다.

빌드·격리 Sandbox 실제 UI·취소/되돌리기·확대/이동·프로세스 재시작 검증 통과. BRAT 업데이트는 사용자 확인 대기입니다.

- [ ] BRAT에서 0.1.10으로 업데이트한 뒤 파일/폴더 삽입 메뉴를 엽니다.
- [ ] 안내와 취소·되돌리기가 Canvas 상단 왼쪽에 보이고 기본 도구막대를 가리지 않는지 확인합니다.

---

# MD Palette 0.1.9 — Canvas 드래그·메뉴 배치 개선

파일을 Canvas에 드래그하면 놓은 위치에 즉시 배치합니다. 메뉴 삽입은 미리보기가 마우스를 따라 이동하고 첫 클릭으로 배치하며 Esc로 취소합니다. 직접 드래그·메뉴 단일 카드는 기존 내용과 겹침을 허용하고, 메뉴 다중 카드 묶음은 겹침을 차단합니다.

- 별도 삽입 확정 버튼을 제거했습니다.
- 기존 카드 본문 위에서도 드롭·미리보기 클릭을 받습니다.
- 큰 폴더의 미리보기는 카드들을 다시 만들지 않고 묶음을 이동합니다.
- Main·원본 파일·가상 폴더 데이터는 유지합니다.

47개 테스트·빌드·격리 Sandbox 실제 동작·프로세스 재시작 통과. 공개 자산은 검증 빌드와 비교합니다. BRAT 업데이트 확인은 사용자 담당입니다.

- [ ] BRAT에서 0.1.9로 업데이트 후 파일을 Canvas에 놓으면 바로 배치되는지 확인합니다.
- [ ] 파일/폴더 삽입 메뉴에서 미리보기가 마우스를 따라오고 클릭 한 번으로 배치되는지 확인합니다.
- [ ] 메뉴의 다중 카드 묶음은 겹치면 배치되지 않고 Esc로 취소되는지 확인합니다.

---

# MD Palette 0.1.8 — Sub 열기 차단·목록 필터·Canvas 삽입

미연결 파일의 Sub 열기를 차단하고 이전 파일·탭을 유지합니다. Card는 목록으로 고정하며 파일 유형을 복수 선택합니다. 파일 목록과 가상 폴더를 기존 Canvas에 위치 지정 → 미리보기 → 겹침 확인 → 확정으로 삽입합니다.

- Card 목록의 Ctrl/Shift 다중 선택 → 우클릭 `현재 Canvas에 삽입…` 또는 Canvas로 드래그합니다.
- Folder 폴더/전체 루트 우클릭 → Canvas 삽입. 폴더 제목·원본 파일 카드·부모 자식 연결선을 배치합니다.
- 겹치면 확정을 막고, 취소·저장 실패 시 변경을 남기지 않습니다. 명령 팔레트에서 마지막 Canvas 삽입을 되돌릴 수 있습니다.
- 기존 Sub 지정 시 연결 승인, Main 변경 시 역할 해제, Folder 보기 형식은 유지합니다. 실제 파일 경로와 원본 내용은 변경하지 않습니다.

47개 테스트·빌드·격리 Obsidian 1.13.7의 실제 UI와 프로세스 재시작 검증 통과. 사용자 본인의 BRAT 확인은 대기입니다.

## 사용자 확인

- [ ] BRAT에서 0.1.8로 업데이트한 뒤 Sub에서 미연결 파일을 열어도 기존 파일이 유지되는지 확인합니다.
- [ ] Card에서 보기 형식 선택이 사라지고 MD+Canvas를 동시에 선택할 수 있는지 확인합니다.
- [ ] 여러 파일을 Canvas로 드래그 → 위치 미리보기 → 확정하여 파일 카드가 생기는지 확인합니다.
- [ ] Folder 폴더 우클릭으로 삽입하여 폴더 제목·파일·연결선이 함께 생기는지 확인합니다.
- [ ] 취소/마지막 삽입 되돌리기와 재시작 후 확정한 배치 유지를 확인합니다.

---

## 이전 MD Palette 0.1.7 — 단일 Sub 지정과 자유 배치

- Sub는 하나의 탭 그룹에 지정되며 파일 종류와 임시 빈 화면에 관계없이 역할을 유지합니다. MD·이미지·Canvas 전환을 검증했습니다.
- 탭 제목 우클릭에 `서브 스페이스로 지정 / 서브 스페이스 지정 해제`, 명령 팔레트에 `서브 스페이스 지정/해제`를 추가했습니다. 해제하거나 다른 그룹을 지정해도 기존 파일·탭은 닫지 않습니다.
- 연결되지 않은 파일을 지정하면 `Main에 연결하시겠습니까?`에서 `연결하고 Sub로 지정 / 취소`를 고릅니다. 이미지도 Main의 link note에 연결됩니다. 취소는 링크와 역할을 바꾸지 않습니다.
- 더블클릭은 Sub 현재 탭 교체, Ctrl+더블클릭은 Sub 새 탭, Ctrl+Shift+더블클릭은 **새 일반 그룹**으로 열기입니다. 대상 Sub에 같은 파일이 있으면 기존 탭으로 이동합니다.
- Sub 아래에 일반 그룹을 놓을 수 있습니다. 전체 높이 강제 재배치를 제거했습니다.
- 이전 버전의 여러 Sub는 마지막 사용 그룹 하나만 Sub로 복원하고 나머지는 일반 그룹으로 남깁니다. 탭·파일은 보존합니다.

44개 테스트·빌드, 격리 Sandbox 실제 UI·업데이트·재시작 검증을 통과했습니다. 실제 Vault 및 다른 플러그인 조합은 사용자 확인 대상입니다.

## 업데이트 후 사용자 확인

- [ ] BRAT 0.1.7 업데이트 후 MD → 이미지 → Canvas를 더블클릭해 같은 Sub와 아이콘이 유지되는지 확인합니다.
- [ ] 탭 제목 우클릭으로 Sub 지정·해제를 시도하고, 미연결 파일의 팝업에서 승인·취소 결과를 확인합니다.
- [ ] Ctrl+더블클릭은 Sub 새 탭, Ctrl+Shift+더블클릭은 Sub 아이콘 없는 새 일반 그룹인지 확인합니다.
- [ ] Sub 아래로 화면을 나누고 재시작해도 배치와 Sub 지정이 유지되는지 확인합니다.

---

## 이전 0.1.6 — 새 링크 파일에 Canvas 추가

`새 링크 파일 추가`에서 Markdown (.md) 또는 Canvas (.canvas)를 선택합니다. 기본값은 Markdown입니다. 선택한 형식으로 빈 파일을 만들고 Main에 연결하며, 자동으로 열지는 않습니다.

- Card·Connections·Folder에서 같은 선택을 제공합니다. Folder에서는 선택한 가상 폴더에 배치합니다.
- 두 형식 모두 Obsidian의 새 노트 저장 위치를 따릅니다. 같은 이름의 MD와 Canvas는 별개이며 기존 동일 형식 파일은 덮어쓰지 않습니다.
- 43개 테스트·빌드·Sandbox 실제 UI·재시작·생성 Canvas 열기 검증 통과.

## 업데이트 후 사용자 확인

- [ ] BRAT에서 0.1.6으로 업데이트하고 `새 링크 파일 추가 → Canvas → 이름 → 만들고 연결`을 누릅니다. Canvas가 연결 목록에 나타나고 탭은 자동으로 열리지 않아야 합니다.
- [ ] 만들어진 Canvas를 더블클릭하면 빈 Canvas가 Sub에서 열려야 합니다.
- [ ] Markdown을 선택하면 기존처럼 빈 MD 파일이 생성·연결되어야 합니다.

---

## 이전 0.1.5 — 각주 드래그 내용 선택

각주 항목을 Main/Sub 본문에 끌어 놓으면 다음을 선택합니다.

- **각주만**: 각주 설명만 놓은 위치에 삽입합니다.
- **본문만**: 위쪽에 표시된 본문 문맥만 삽입합니다.
- **각주와 본문**: 본문 뒤에 새 각주 번호를 붙이고 각주 설명을 문서 아래에 연결합니다. 기존 번호와 겹치지 않으며 한 번의 실행 취소로 함께 제거됩니다.

Sub Canvas에서도 같은 선택으로 텍스트 카드를 만듭니다. 원문을 이동하거나 삭제하지 않으며 Markdown 서식은 유지합니다.

## 업데이트 후 사용자 확인

- [ ] BRAT에서 0.1.5로 업데이트하고 각주를 Sub 본문에 놓아 세 선택지가 나타나는지 확인합니다.
- [ ] `각주만`과 `본문만`이 각각 아래쪽 각주 설명과 위쪽 본문 문맥만 넣는지 확인합니다.
- [ ] `각주와 본문`이 본문에 각주 번호를 붙이고 설명을 문서 아래에 연결하는지 확인합니다.

---

## 이전 0.1.4 — 메타데이터 글자 크기와 Sub 드래그 개선

Metadata View 안에서 Ctrl+휠 위로 글자를 키우고, Ctrl+휠 아래로 줄일 수 있습니다. 10~32px 범위에서 1px씩 바뀌며 재시작 후에도 유지됩니다. 각주·문맥·메타데이터 내용과 각주 편집창에 적용됩니다.

Sub 문서에서 다른 줄로 이동하자마자 놓으면 삽입이 취소되던 드래그 문제를 수정했습니다. 드래그 위치 표시를 재사용하고, 내용이 바뀌지 않은 긴 문서 전체를 반복해서 읽는 작업을 줄였습니다.

- 일반 휠 스크롤, 각주 편집 중 입력·커서, 기존 원문·문서별 정리를 유지합니다.
- 41개 테스트·빌드, Obsidian 1.13.7 Sandbox Ctrl+휠·Sub Source mode/Live Preview 삽입·재시작 확인 통과.
- 805,407글자 Sub와 메타데이터 1,503개 시험에서 이동 처리 121회 동안 전체 문서 읽기 1회, 위치 표시 요소 2개 재사용을 확인했습니다. 해당 PC·시험 조건의 관측입니다.

## 업데이트 후 사용자 확인

- [ ] BRAT에서 0.1.4로 업데이트한 뒤 Metadata View 본문에 마우스를 올리고 Ctrl+휠 위/아래를 굴립니다. 글자가 커지고 작아져야 합니다.
- [ ] 각주 또는 Highlight를 Sub 문서의 원하는 줄로 옮겨 놓습니다. 삽입 메뉴가 나타나고 선택한 위치에 내용이 들어가야 합니다.
- [ ] Obsidian 재시작 후 선택한 글자 크기가 유지되는지 확인합니다.

---

## 이전 0.1.3 — 각주·메타데이터 Markdown 표시

Metadata View의 각주 본문과 본문 문맥, Highlights·Tasks·Block Reference·웹 링크 제목에 Markdown 서식을 표시합니다. `**굵게**`, `==강조==`, 목록과 링크를 읽기 편한 모습으로 보여주며 테마 색상을 따릅니다.

- 각주 편집에서는 Markdown 원문을 수정하고, 저장하면 서식이 적용된 표시로 돌아옵니다.
- 기존 원문 위치 이동, URL 열기 메뉴, 할 일 체크와 드래그 재사용을 유지합니다.
- Card 미리보기처럼 임베드는 링크로 표시합니다. 원문 내용은 표시만으로 바뀌지 않습니다.
- 41개 테스트·빌드, Obsidian 1.13.7 격리 Sandbox의 실제 클릭·편집·검색·드래그 및 프로세스 재시작을 통과했습니다.

## 업데이트 후 사용자 확인

- [ ] BRAT에서 MD Palette를 0.1.3으로 업데이트하고 Metadata View → 각주를 엽니다. 굵게·강조·링크가 서식으로 보여야 합니다.
- [ ] 각주의 편집 → 저장을 누릅니다. 편집창에는 원문이 보이고 저장 후에는 서식이 적용되어야 합니다.
- [ ] Obsidian을 재시작합니다. 같은 각주가 서식을 유지해 보여야 합니다.

---

## 이전 0.1.2 — 드롭 대상 줄 강조

자료를 문서로 드래그하면 실제 삽입될 줄 전체에 옅은 강조색 배경이 표시되고, 정확한 글자 위치에는 세로 커서가 함께 표시됩니다. 색상은 현재 Obsidian 강조색을 따릅니다.

- 마우스를 움직이면 대상 줄과 글자 위치가 함께 바뀝니다.
- 삽입 메뉴가 열린 동안 유지되며, 삽입·취소 후 두 표시가 함께 사라집니다.
- 빈 줄·자동 줄바꿈된 문장·문서 끝, Source mode와 Live Preview에서 확인했습니다.

---

## 이전 0.1.1 — 글자 사이 드롭 위치 표시

Main/Sub 문서로 자료를 끌어 놓을 때 실제 삽입될 글자 사이에 강조색 세로 커서가 표시됩니다. 메뉴에서 삽입 방식을 고르는 동안에도 위치를 유지합니다.

- 같은 줄의 글자 사이, 빈 줄, 자동 줄바꿈된 문장과 문서 끝을 가리킵니다.
- Esc·메뉴 취소·드래그 종료 시 필요 없는 표시를 제거합니다.
- 실제 Sandbox 드래그, Source mode·Live Preview, 프로세스 재시작 및 41개 테스트를 통과했습니다.
- BRAT 업데이트 후 0.1.1을 확인하고, Metadata 링크를 문장 중간에 놓아 세로 커서 위치에 들어가는지 확인하세요.

---

## 이전 0.1.0 — 최종 통합 검증

Main/Sub, 연결 파일 정리, 가상 폴더, Metadata와 드래그 재사용을 함께 확인한 통합 버전입니다. 기존 화면과 사용 방법은 유지됩니다.

- Obsidian 1.13.7의 격리 Sandbox에서 실제 클릭·드래그·오류 보호·업데이트·앱 재시작을 확인했습니다.
- 0.0.15에서 라벨·문서별 폴더·탭·역할과 기존 시험 파일 2,017개를 보존했습니다.
- 41개 테스트와 빌드를 통과했습니다. 연결 파일 2,000개와 Metadata 1,500개로 입력·목록 반응을 측정했습니다. 모든 PC의 성능을 보장하는 수치는 아닙니다.
- 플러그인 기능 소스 변경은 없습니다. BRAT 업데이트 확인은 사용자가 담당합니다.

## 업데이트 후 사용자 확인

- [ ] BRAT 업데이트 후 MD Palette 버전이 0.1.0인지 확인합니다.
- [ ] Main에서 연결 파일을 더블클릭합니다. 오른쪽 Sub가 열리고, 다른 파일은 같은 Sub 탭에서 바뀌어야 합니다.
- [ ] 다른 Main을 지정했다가 돌아옵니다. 문서별 가상 폴더와 라벨이 유지되어야 합니다.
- [ ] Metadata의 Task를 누르거나 자료를 Sub에 끌어 놓습니다. 선택한 동작만 반영되어야 합니다.
- [ ] Obsidian을 재시작합니다. Main/Sub와 탭·정리 상태가 유지되어야 합니다.

---

## 이전 0.0.15 — Sub 전체 높이 배치

Main이 영상 등 다른 파일과 위아래로 배치된 경우, Sub가 오른쪽 전체 높이를 사용하도록 수정했습니다.

- 왼쪽의 영상 위·Main 아래 배치를 유지하고, Sub는 묶음 전체의 오른쪽에서 열립니다.
- 이미 아래쪽에 열려 있던 Sub도 전체 높이로 정리합니다. 기존 파일과 탭은 유지합니다.
- 기본 더블클릭의 Sub 탭 교체, Ctrl+더블클릭 새 탭, Ctrl+Shift+더블클릭 새 Sub 그룹 동작을 유지합니다.

41개 테스트·빌드·Obsidian 1.13.7 Sandbox 실제 클릭·참조 배치 비교·프로세스 재시작을 통과했습니다. 기존 파일 2,065개와 라벨·가상 폴더·탭 복원을 확인했습니다. BRAT 확인은 사용자가 담당합니다.

## 업데이트 후 확인

- [ ] BRAT으로 0.0.15 업데이트 후 영상과 Main을 위아래로 놓고 연결 파일을 더블클릭합니다. Sub가 오른쪽 전체 높이로 열려야 합니다.
- [ ] 다른 파일을 더블클릭합니다. 같은 Sub에서 파일이 바뀌고 왼쪽 영상·Main 배치는 유지되어야 합니다.
- [ ] Obsidian을 재시작합니다. 왼쪽 위아래 배치와 오른쪽 Sub가 유지되어야 합니다.
