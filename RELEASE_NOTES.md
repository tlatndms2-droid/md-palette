# MD Palette 0.0.14 — 새 링크 파일 추가

기존 파일을 연결하는 기능에 더해, 새 Markdown 파일을 만들면서 현재 Main에 바로 연결할 수 있습니다.

- Card·Connections 상단 `새 링크 파일 추가` → 이름 입력 → 저장 위치 확인 → `만들고 연결`.
- 저장 위치는 Obsidian의 ‘새 노트를 만들 위치’ 설정을 따릅니다. ‘현재 파일과 같은 폴더’는 Main 폴더 기준입니다.
- Folder 빈 공간 우클릭에서도 생성할 수 있으며 현재 가상 폴더에 함께 배치합니다.
- 같은 이름의 파일은 덮어쓰지 않습니다. 취소·Main 변경·저장 실패를 처리하며, 생성한 항목을 더블클릭하면 Sub에서 열립니다.

38개 테스트·빌드·Obsidian 1.13.7 Sandbox 실제 클릭·세 가지 저장 위치·실패 복구·프로세스 재시작 통과. 기존 파일 2,058개와 라벨·문서별 폴더 보존 확인. BRAT 확인은 사용자 담당입니다.

## 업데이트 후 확인

- [ ] BRAT으로 0.0.14 업데이트 후 `Link View → Card → 새 링크 파일 추가`에서 이름을 입력하고 `만들고 연결`을 누릅니다. 새 파일이 목록에 나타나야 합니다.
- [ ] 입력 창에 표시된 저장 위치가 Obsidian의 새 노트 저장 설정과 일치하는지 확인합니다.
- [ ] Folder의 빈 공간 우클릭 → `새 링크 파일 추가`로 만든 파일이 현재 가상 폴더에 나타나는지 확인합니다.
- [ ] Obsidian을 재시작해 파일과 Main 연결이 유지되는지 확인합니다.
