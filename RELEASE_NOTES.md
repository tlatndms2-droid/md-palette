# 0.0.8 — Main·Sub와 문서별 가상 폴더

- Reference Space와 Main/Sub 교환을 제거했습니다. Markdown·이미지·Canvas·PDF·영상 등 지원 파일은 모두 Sub에서 열립니다.
- Sub에서는 다른 파일을 새 탭으로 추가하고, 같은 파일은 이미 열린 탭으로 이동합니다.
- 새 Main 지정 시 기존 Main·Sub 탭을 남기고 이전 역할 아이콘을 제거합니다. Main 탭 우클릭에 지정 해제 메뉴를 추가했습니다.
- Card와 Folder의 파일 제목을 카드 위로 옮기고 크게 표시했습니다. Markdown 미리보기는 제목·강조·목록·링크를 렌더링합니다.
- 가상 폴더와 파일 배치를 Main 문서마다 따로 기억합니다. 다른 Main에는 나타나지 않고 원래 문서로 돌아오면 복원됩니다.
- 대량 카드 생성 중 불필요한 화면 계산과, Main의 일반 문장 입력 시 변하지 않은 카드의 재생성을 줄였습니다.

**업데이트 시 기존 테스트용 전역 가상 폴더와 배치 정보는 비웁니다.** 실제 파일·본문·연결·라벨은 남습니다. 새로 만든 문서별 가상 폴더는 재시작·다음 업데이트에서 초기화하지 않습니다.

TypeScript·빌드·19개 자동 테스트, Obsidian 1.13.7 격리 Sandbox의 실제 메뉴·클릭·드래그, 저장 실패·문서 전환 중 취소, 2,000개 연결 자료, 최종 빌드 프로세스 재시작과 기존 파일 2,020개 해시 검증을 통과했습니다.

사용자 확인:

- [ ] BRAT에서 0.0.8 업데이트 후 이미지와 Canvas를 열면 Sub에 각각 새 탭이 생기고 다시 열 때 기존 탭을 보여줍니다.
- [ ] 다른 Main 지정 또는 Main 탭 우클릭 → 지정 해제 후 기존 탭은 남고 이전 역할 아이콘은 사라집니다.
- [ ] Card와 Folder에서 파일 제목이 위에 크게 보이고 Markdown 미리보기가 읽기 편하게 표시됩니다.
- [ ] A 문서에서 만든 가상 폴더는 B에 나타나지 않고, A로 돌아오거나 재시작하면 복원됩니다.

5단계 Metadata와 6단계 재사용 드래그는 이번 수정에 포함하지 않습니다.
