## 0.0.3 — 연결 파일 Card

Main을 지정한 뒤 `Link View → Card`에서 연결 파일을 카드로 보고 정리할 수 있습니다.

- 역링크와 나가는 링크를 합쳐 같은 파일은 한 번만 표시하고 Main 자체는 숨깁니다.
- `연결 파일 추가`로 고른 기존 파일은 Main 상단 **`link note` 속성**에 저장합니다. 본문 링크 목록을 만들지 않으며 중복·취소·저장 실패 시 불필요한 변경을 남기지 않습니다.
- Markdown 본문, PDF 첫 페이지, 이미지, Canvas 축소 그림, 영상 첫 프레임을 표시합니다. 읽을 수 없는 파일은 아이콘과 이름으로 표시합니다.
- 단일·Ctrl 다중·Shift 범위 선택, 더블클릭 열기, 카드 우클릭 메뉴, 선택 카드 묶음 순서 변경을 제공합니다. Markdown은 Sub, 다른 파일은 Reference에서 열립니다.
- 파일당 라벨 하나, 이름·색 변경, 일괄 적용·제거, 여러 라벨 필터를 제공합니다. 라벨은 Markdown에 기록하지 않습니다.
- 보기 형식 6종, 글자 크기 3종, Ctrl+휠 전환과 필터 접기를 지원합니다. 순서·라벨·필터·보기 설정은 재시작 후 복원됩니다.

Obsidian 1.13.7 이상, Desktop 전용입니다. 기존 Space·탭·설정 데이터를 보존합니다. 격리 Sandbox에서 실제 화면·드래그·재시작·원문 보존을 확인했습니다. 2,013개 파일/411개 연결 카드 시험에서 전체 다시 그리기는 약 101ms였습니다. 자동 조작 환경의 제한된 시험값이며 일반 사용 속도를 보장하는 수치는 아닙니다.

Connections·Folder·Metadata와 Main 본문으로의 카드 드롭은 후속 단계입니다. Main 링크 클릭 팝업은 계속 보류합니다.

사용자 확인:

- [ ] BRAT에서 업데이트 → MD Palette 버전 `0.0.3` 확인.
- [ ] Main 지정 → Card → 연결 파일 추가: 카드가 나타나고 Main의 `link note` 속성에 저장됩니다.
- [ ] Ctrl로 여러 카드 선택 → 우클릭 → 새 Label 만들기: 선택한 카드에만 같은 라벨이 붙습니다.
- [ ] 선택 카드를 드래그: 묶음 내부 순서를 유지한 채 이동합니다. 더블클릭하면 해당 Space에서 열립니다.
- [ ] 필터·보기 형식을 바꾼 뒤 재시작: 라벨·순서·보기 설정이 유지됩니다.

사용자 확인을 받은 뒤 다음 Connections 단계로 진행합니다.
