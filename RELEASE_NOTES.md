# 0.0.7 — Folder View

`Link View → Folder`에서 연결 파일을 실제 Vault 경로와 독립적인 가상 폴더로 정리할 수 있습니다. Main을 바꿔도 폴더 구조와 파일의 가상 위치는 그대로 유지됩니다.

- 복합뷰(Tree + Folder) / Tree뷰 / Folder뷰, 상하·좌우 분할과 영역 크기 저장.
- 가상 폴더 생성·이름 변경·드래그 이동·삭제. 삭제하면 바로 안의 파일과 하위 폴더를 한 단계 위로 옮기며 실제 파일은 삭제하지 않습니다.
- 단일·Ctrl·Shift 선택, 묶음 이동, 상위 경로·빈 공간 Drop, 자기 자신·하위 폴더 이동 방지.
- 뒤로·앞으로·위로·경로 탐색, 현재 폴더와 하위 폴더 이름 검색, 6가지 표시 형식.
- Folder 정렬: 사용자 지정 / 이름 / 유형 / 수정 날짜 / 크기 및 자동 정렬의 오름차순·내림차순. Tree 정렬: 사용자 지정 / 이름. 자동 정렬에서도 다른 폴더로 이동할 수 있으며 저장된 사용자 지정 순서는 보존합니다.
- Card와 파일 유형 필터 공유. 기존 라벨을 표시하고 기존 Card·Connections 설정을 보존합니다.
- 빈 공간 우클릭 → 연결 파일 추가 → Main의 `link note` 연결과 현재 가상 폴더 배치를 함께 처리합니다. 이미 다른 가상 위치에 배치된 파일은 연결·이동 모두 취소합니다.
- 대량 목록은 보이는 구간부터 이어 표시하며 검색은 전체 자료를 대상으로 합니다. 검색어는 재시작과 Main 변경 시 비웁니다.

TypeScript 검사, 18개 자동 테스트, Obsidian 1.13.7 Sandbox 실제 클릭·드래그, 저장 실패와 본문 변경 실패, 2,000개 연결 자료, 좁은 화면·밝고 어두운 테마, 프로세스 재시작 및 원본 파일 해시 검증을 통과했습니다.

사용자 확인:

- [ ] BRAT에서 0.0.7 업데이트 → `Link View → Folder`가 표시됩니다.
- [ ] 빈 공간 우클릭으로 폴더를 만들고 파일을 드래그 → 실제 Vault 경로는 유지됩니다.
- [ ] `Folder Contents` 오른쪽 정렬 메뉴 → 모든 정렬 기준과 방향을 선택할 수 있습니다.
- [ ] `보기 형식`에서 복합뷰·Tree뷰·Folder뷰를 전환 → 같은 폴더 구조를 봅니다.
- [ ] Obsidian 재시작 → 가상 위치·보기·정렬 상태가 유지됩니다.

4단계 버전입니다. Metadata 및 본문·Canvas에 재사용하는 드래그 기능은 후속 단계입니다. 사용자 본인의 BRAT 확인 전에는 5단계를 시작하지 않습니다.
