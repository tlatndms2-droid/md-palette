# 0.0.8 수정 검증

2026-09-20 · Obsidian 1.13.7 · 격리 Sandbox · 백그라운드 CDP

## 검증한 사용자 흐름

- Card에서 Markdown·이미지·Canvas·PDF·영상 더블클릭 → 같은 Sub에 각각 새 탭. 이미 열린 파일은 기존 탭으로 이동.
- Connections 목록, Obsidian 기본 그래프의 Canvas 노드 클릭·우클릭, 파일 메뉴, Sub 파일 선택 명령 → Sub 열기. Reference 및 교환 명령 제거 확인.
- 다른 Main 탭 우클릭 지정 → 기존 탭 모두 보존, 이전 Sub 아이콘 제거. 새 Main의 Sub를 별도로 생성.
- Main 탭 우클릭 지정 해제 → 탭 모두 보존, 역할 아이콘 제거, 사이드바 Main 없음.
- Card와 Folder의 파일 제목이 미리보기 위에 있고 본문보다 큰지 확인. Markdown 제목·강조·목록·링크 표시 및 연결 문서 편집 후 미리보기 갱신 확인.
- 여섯 표시 형식의 제목 배치, 240px 사이드바 가로 넘침 없음, 카드 다중 선택·실제 드래그·Esc 취소·기존 DOM 유지 확인.
- A와 B에 연결된 같은 파일을 다른 가상 폴더로 드래그. Main 변경 시 각 폴더만 보이고 복귀 시 위치·현재 폴더 복원. 탐색 기록은 문서 전환 시 비움.
- 모든 Folder 정렬 기준·방향 선택 후 수동 순서 보존 확인. Tree·Folder 컨트롤 배치는 기존 참조 구조 유지.
- Main·연결 파일 이름 변경 후 문서별 가상 배치 유지. Main이 바뀐 뒤 오래된 생성 창에서 저장하는 동작 차단.
- 저장 실패, 링크 추가 실패, 저장 도중 Main 변경을 주입하고 가상 위치 전체 취소 및 다른 Main 데이터 보호 확인.

## 업데이트·재시작·자료 보존

- 0.0.7 설치본과 `.obsidian`을 먼저 백업한 후 0.0.8 설치. 기존 전역 폴더·배치 초기화, Card 라벨·필터·Connections·기타 데이터 보존 확인.
- 전용 Sandbox 프로세스를 종료·재실행하고 새 CDP 대상을 탐색. 최종 0.0.8과 Main/Sub 역할·아이콘, 이미지·Canvas·영상만 있는 Sub, 문서별 폴더·배치·설정 복원 확인.
- Obsidian이 지연 로딩한 이미지 탭도 재시작 후 다시 열면 중복 생성 없이 활성화됨을 실제 더블클릭으로 확인.
- 기존 Sandbox 파일 2,020개 경로·SHA-256 일치. 실제 작업 Vault에는 설치·테스트하지 않음.
- 최종 로컬 빌드와 Sandbox 설치 자산 세 개의 SHA-256 일치. TypeScript 검사·빌드·자동 테스트 19개 통과.

## 대량 자료 측정

2,000개 연결 자료에서 화면 생성 시간을 측정했다. 일반 사용자 환경의 고정 지연을 보장하는 수치는 아니다.

- Folder: 첫 80개씩 표시, 측정값 약 45~159ms.
- Card: 전체 2,000개 카드 생성 약 947ms → 약 296ms. 미리보기는 보이는 13개부터 렌더링.
- Main의 연결 관계·연결 파일 내용이 변하지 않은 일반 문장 수정에는 기존 카드 DOM을 유지한다.
- 안정된 동일 편집 화면의 입력 왕복 비교: 플러그인 활성 118ms / 비활성 58ms. 여러 탭 그룹이 쌓인 112px 폭 편집 영역에서 측정했으므로 일반 작업 공간의 입력 지연으로 해석하지 않는다.

## 증거와 배포 상태

- `.artifacts/revision/`: 사전 백업, 원본 해시, migration/ui/checks/performance/cards/restart 결과, 화면, 최종 자산 해시.
- `scripts/revision-*.mjs`: 해당 Sandbox 전용 재현 스크립트.
- 검증 과정의 필터 입력값·좁은 탭 좌표·지연 로딩 탭 판독 문제는 검증 스크립트에서 바로잡고 해당 확인을 다시 수행했다.
- 공개 Release 및 BRAT 업데이트 검증은 진행 중이며 결과를 확인한 후 갱신한다.
- Sandbox와 시험 자료·설정을 유지한다. 사용자 본인 확인은 자동 검증과 별도로 기다린다. 5~7단계는 미시작이다.
