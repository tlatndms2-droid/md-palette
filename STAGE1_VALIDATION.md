# 1단계 Space·사이드바 검증

버전: 0.0.2 / 검증 환경: Windows, 격리 Obsidian 1.13.7 / 2026-09-18.

## 확정 범위

사용자가 1단계 진행을 요청했다. 자료 비교 중 Main 본문 링크 클릭 팝업의 문서·이미지 차이를 확인했고, 사용자는 **팝업을 보류하고 문서의 Space 기능만 구현**하도록 확정했다.

Space 및 사이드바 뼈대만 구현했다. Card·Connections·Folder·Metadata 내용, 연결 추가, 가상 폴더, 원문 편집 및 드래그 재사용은 해당 후속 단계 범위다. 일반 파일 선택 명령어와 파일 우클릭 메뉴로 Space 열기를 제공한다.

## 구현과 확인 결과

- 기본 빌드·TypeScript 및 패키지/저장 상태 검사 4개.
- 실제 탭 제목 우클릭 메뉴에서 Main 지정 및 Main/Sub 전환. 활성 파일만 교환하고 비활성 탭·그룹을 보존.
- 실제 파일 선택창에서 Sub 재사용, Reference의 Markdown·Canvas·이미지 열기와 동일 파일 중복 방지.
- 활성 탭을 바꾸면 역할 아이콘도 새 활성 탭으로 이동. 사이드바 Main 기준 갱신.
- Main에서 비-Markdown 활성 시 역할은 유지하고 Main 기준 동작 비활성. Markdown으로 돌아오면 복구.
- 새 Main 지정: 이전 Main 일반 그룹 유지, Sub의 Obsidian 기본 새 탭 상태 및 위치 재사용, 기존 Reference 닫기. 파일 삭제 없음.
- Sub·Reference의 마지막 파일을 닫으면 역할 종료. Main 그룹을 닫으면 전체 역할만 해제하며 자동 successor 없음.
- 실제 마우스 드래그로 일반 탭을 사이에 옮긴 뒤 관리 그룹 순서 복구. 일반 탭과 원본 파일 보존.
- 실제 하단 분할 메뉴로 중첩 분할을 만든 뒤에도 관리 그룹을 좌우 순서로 유지하며 기존 탭 보존.
- 파일 선택 취소 시 변화 없음. 파일 열기 실패를 주입한 Main/Sub 전환 및 새 Main 준비 실패에서 기존 탭·역할 유지.
- 밝은/어두운 테마, 활성 탭 역할 아이콘 및 사이드바 구조를 참조 이미지와 대조. 220px 사이드바에서 수평 넘침 없음.
- 최종 0.0.2 프로세스 재시작 후 기존 그룹·파일·역할·사이드바 보기 선택 복원.
- 저장된 Sub가 사라졌을 때 재생성하지 않음. Main이 사라졌을 때 나머지 역할만 단독 복원하지 않음.
- 기존의 알 수 없는 데이터 필드를 보존. 이 단계에서 Markdown 본문·실제 파일 경로를 수정하는 기능은 없음.

시험은 백그라운드 CDP로 실행했다. 창이 비활성일 때 Electron 타이머가 지연되므로 UI 상태가 안정된 뒤 검사했다. 이 대기 시간은 사용자 클릭 반응 속도 측정값이 아니다.

## 성능과 기술 경계

파일 본문 감시·Vault 전체 스캔·타이머 반복 작업은 등록하지 않는다. 작업 공간 변경 시 열려 있는 그룹을 확인하고 저장을 모아서 처리한다. 파일 선택창을 열 때 Vault 파일 목록을 읽는다. 대형 Vault 성능 수치는 아직 측정하지 않았다.

그룹 직접 이동에는 공개 타입에 없는 Obsidian 메서드가 필요하여 `workspace-adapter.ts`에 한정하고 호출 가능 여부를 확인한다. 현재 검증은 같은 데스크톱 창의 일반/중첩 분할을 대상으로 했다. 별도 팝아웃 창 사이 이동은 이번 검사 범위가 아니다.

원시 JSON·스크린샷·원본 설정 백업은 `.artifacts/stage1/`에 보관한다. 원본 기획 문서·이미지·ZIP은 변경하지 않았다.

## 배포 및 사용자 확인

Release 자산 및 BRAT 업데이트 결과는 확인 후 이 문서와 HANDOFF에 기록한다. 사용자 본인의 BRAT 확인은 자동 검증과 별도이며, 확인 전 2단계를 시작하지 않는다.
