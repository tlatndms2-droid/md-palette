# MD Palette 프로젝트 작업 지침

## 적용 범위와 기준 문서

- 이 파일은 MD Palette 저장소 전체에 적용한다.
- 현재 사용자 요청과 최신 확정 답변이 가장 우선한다.
- 작업 시작 전 `HANDOFF.md`, `IMPLEMENTATION_PLAN.md`, 현재 Git 상태를 확인한다.
- 기능과 사용자 흐름은 `MD_Palette_Planning_Pack_Final`의 문서 및 UI Reference를 기준으로 한다.
- 문서와 이미지가 기능·버튼·동작 면에서 충돌하면 임의로 선택하지 말고 해당 영역 구현 전에 사용자에게 확인한다.
- 원격 저장소만 있어 Planning Pack 또는 참조 이미지가 없다면 전체 기능을 추정하지 말고 원본 자료를 먼저 확보한다.

## 단계 진행 규칙

- 전체 기능을 한 번에 구현하지 않는다. `IMPLEMENTATION_PLAN.md`의 0단계부터 순서대로 진행한다.
- 한 단계의 흐름은 `구현 → 관련 테스트·빌드 → Sandbox 실제 UI 및 재시작 검증 → GitHub Release → 사용자 BRAT 확인`이다.
- 사용자 BRAT 확인 전에는 다음 단계 구현을 시작하지 않는다.
- 단계 일부가 실패·미수행·판정 불가이면 해당 단계를 완료로 보고하거나 다음 단계로 넘어가지 않는다.
- 예상 밖의 작업이 필요하면 이유, 사용자 화면에 미치는 영향, 추가 예상 시간을 알리고 범위 확대 전에 확인받는다.
- 사용자 요청 없이 병렬 에이전트나 별도 작업으로 기능 구현을 분산하지 않는다.

## 구현 불변조건

- Space는 기존 Obsidian Tab Group에 부여하는 역할이며 독자 편집기를 만들지 않는다.
- 관리 순서는 `Main → Sub → Reference`이며 일반 Tab Group을 닫거나 삭제하지 않는다.
- Main/Sub 전환은 활성 파일만 교환하고 비활성 Tab과 그룹 역할·위치를 보존한다.
- Virtual Folder 구조와 파일의 가상 위치는 MD Palette 전체의 전역 Plugin Data다.
- 파일 하나는 가상 위치 하나만 가지며 Main 변경으로 Virtual Folder 구조를 재구성하지 않는다.
- Main은 현재 표시할 연결 파일의 기준일 뿐 Virtual Folder의 별도 사본을 만들지 않는다.
- Label과 Virtual Folder 조작으로 Markdown 내용이나 실제 Vault 경로를 바꾸지 않는다.
- 실제 Markdown 수정은 기획서가 허용한 `link note`, 각주 내용, Task checkbox 및 명시된 삽입 동작으로 제한한다.
- 실패·취소·지원하지 않는 Drop에서는 원본과 대상에 부분 변경을 남기지 않는다.
- 이전 단계에서 저장된 사용자 설정과 정리 상태를 업데이트 과정에서 보존한다.

## UI 구현 규칙

- 제공 UI Reference의 화면 구조, 배치, 상대 비율, 간격, 정보 밀도, 시각 계층을 구현 기준으로 사용한다.
- 색상은 하드코딩된 특정 색 대신 Obsidian 테마와 사용자 강조색을 따른다.
- 문서에 없는 순수 시각 세부는 참조 이미지를 따를 수 있다.
- 이미지에만 존재하는 새 기능이나 동작은 사용자 확인 없이 구현하지 않는다.
- 사용자에게는 내부 구조보다 화면에서 누르는 위치, 보이는 변화, 저장 후 상태를 기준으로 설명한다.

## 검증 규칙

- 실제 작업 Vault에서 설치·테스트하지 않는다. 별도의 Obsidian Sandbox만 사용한다.
- Sandbox가 없으면 `obsidian-sandbox-open`을 먼저 사용하고, 열린 대상에서 `obsidian-sandbox-validation`으로 설치·검증한다.
- 실제 UI 검증은 백그라운드 CDP 경로를 기본으로 하며 Computer Use로 임의 전환하지 않는다.
- 코드·테스트·빌드 성공만으로 UI 구현 완료라고 보고하지 않는다.
- 각 단계에서 해당 화면의 버튼·메뉴·선택·드래그, 참조 이미지 일치성, 핵심 동작, 필요한 저장 상태와 재시작 복원을 확인한다.
- 테스트 자료와 설정은 검증 전 백업하고 검증 후 복원 및 해시를 확인한다.
- 한 실행 방법의 실패를 곧바로 `UI 미검증`으로 처리하지 않는다. 관련 Sandbox 스킬의 안전한 대체 경로를 수행한다.
- 필수 UI 검증을 완료할 수 없으면 Release를 만들지 않고, 시도한 경로·정확한 실패·미검증 항목을 보고한다.
- 대형 Vault 성능 수치는 측정 전 확정하지 않는다. 연결 파일·Metadata가 많은 시험 자료로 화면 멈춤과 타이핑 방해 여부를 확인한다.

## Git·Release·BRAT 규칙

- 사용자가 만든 파일과 무관한 변경을 덮어쓰거나 정리하지 않는다.
- 단계별 변경은 검증 가능한 단위로 커밋하고 공개된 버전·태그를 덮어쓰지 않는다.
- 첫 설치 가능 버전은 `0.0.1`부터 순차 증가시키며 최종 통합 버전은 `0.1.0`으로 한다.
- 코드 변경 단계는 최종 버전으로 Sandbox 기능 및 재시작 검증을 통과한 뒤 GitHub Release를 만든다.
- Release에는 같은 검증 빌드의 `main.js`, `manifest.json`, `styles.css`를 올리고 로컬·설치본·릴리즈 자산의 버전과 해시를 확인한다.
- Release 후 BRAT 설치·활성화 또는 업데이트를 확인하고, 사용자가 직접 확인할 이번 단계 전용 체크리스트를 제공한다.
- 사용자 본인의 BRAT 확인을 자동 검증 결과로 대신하지 않는다. 확인 답변을 받은 뒤 다음 단계로 진행한다.
- 문서만 변경한 작업에는 플러그인 Release나 BRAT 검증을 추가하지 않는다.

## 보호 대상과 제외 범위

- `MD_Palette_Planning_Pack_Final`, `md palette ui image`, `MD_Palette_Planning_Pack_Final.zip`은 원본 자료다. 명시적 요청 없이 수정·이동·삭제하지 않는다.
- Mobile, 독자 Markdown Editor, 실제 Vault Folder 재구성, 자동 parent/child hierarchy, 전체 작업 데이터 초기화는 1차 범위에 포함하지 않는다.
- 실제 Vault 파일 삭제·이동은 구현하지 않는다.

## 완료 보고

- 완료 보고 전에 현재 요청, 해당 단계 완료 조건, 실제 수행 결과를 대조한다.
- 통과한 사용자 화면 흐름, 검증한 버전, 재시작 결과, Release와 BRAT 상태를 구분해 보고한다.
- 미수행·실패·판정 불가 항목을 빠뜨리지 않는다.
- 사용자가 다음 단계로 넘어가도 된다고 확인하기 전에는 현재 단계에서 멈춘다.
