{
  "name": "md-palette",
  "version": "0.0.2",
  "private": true,
  "description": "MD Palette for Obsidian — Spaces and sidebar",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.0"
  }
}
